# 帧动画编辑器升级计划

目标：将现有七页改为导入/抠像/编辑/精灵图/导出五页。编辑为画布+多轨时间线中心布局；Root/Motion/Align以编辑页内“对齐/时间线”标签及折叠属性保留。项目标准画布规则先于所有编辑，旧工程无编辑数据时行为不变。

阶段1：非破坏性模型与输出契约
- 源帧索引不变；每个时间线帧块独立ID、source_index、track、start、duration、关键帧与速度标记。
- frame_overrides按块ID保存位移/比例/透明度，复制块互相独立；4轨可见/锁定。删除/复制/移动/倒放/重定时保留指令记录和可撤销快照。
- 数量模式39→20用曲线帧重映射，首尾与用户关键帧优先；速度/目标时长模式编辑真实帧duration，允许区段处理。
- Linear/Ease In/Out/InOut/单调Custom Bezier；自定义曲线图编辑控制柄/数值，位置/比例/透明度关键帧插值。
- 最终输出按多轨时间边界合成，一帧一份duration；Preview Source FPS按duration播放，JSON导出duration，精灵图仍只打包像素。

阶段2：唯一最终帧提供器
- 原raw/keyed/source_aligned/aligned保留作为素材与自动对齐结果；新增final阶段用于时间线取帧、位移、合成。
- final签名依赖align签名+编辑模型；FinalFrameProvider同时提供像素、源索引映射、时长、最终FrameData。Preview/Sheet/PNG/Godot使用同一结果。
- 原video.frame_count和tracking_results始终为源帧；导出视图使用编译后帧数/时序，不能覆盖源信息。RGBA素材出口仍不应用时间线。

阶段3：五页编辑器UI
- 顶栏统一新建/打开/保存/导入/撤销/重做/导出；5个明确页面。
- 编辑页中上画布、中下多轨时间线，左资源/动画/轨道，右对齐/时间线属性。
- 画布轴/原点/ground/root/bounds/reference/safe区域、拖动和键盘微移/Shift、数值与批量偏移，洋葱皮/残影数量和透明度。
- 时间线点击/Ctrl/Shift/框选、块拖动移动/重排、复制粘贴指定位置或末尾、删除、倒放、缩放/滚动/播放头、关键帧、轨道可见/锁定。
- 工具区目标帧数、速度倍率、目标时长、曲线与首尾/关键帧保护。可在编辑页直接播放/暂停/停止/循环/步进/局部预览FPS。
- 主色提高亮度和分区/文字/输入框对比，选中与hover不同；125%/150%原生Qt验收。

阶段4：回归与交付
- 39→20/首尾/关键帧、单多选删/复制/移/倒放、区段时间、Bezier、多轨排序和合成、手动偏移、Undo/Redo（含Root和设置）、旧工程和项目标准画布。
- 统一Final Preview/Export逐像素与时长验证、保存/重开、真实Qt三条用户工作流及Windows冻结成品。
- 更新README/ARCHITECTURE/TASKS/VALIDATION和约束文件；全部通过后发布独立新版目录，不影响仍运行的旧程序。

边界：不会修改或删除用户源文件；项目适配只按分辨率中心；后续手动移动属于明确编辑。自动跟踪算法保留，UI入口合并，不重新创建另一套Root/Canvas算法。参考叠加不得意外写入PNG。

三角色预审：架构分离编辑模型/编译渲染/UI命令；数据流区分源帧数与编辑帧数、源索引与实例ID、秒与帧、源坐标与输出坐标；工程重点处理取消/缓存失效、轨道锁定、空时间线、关键帧数量大于目标、旧项目缺省、撤销合并与Qt线程回收。


实施记录（2026-09-15）：阶段1～3全部接通；阶段4最终147项pytest通过，655条汉化覆盖通过。原生150%三条用户工作流与旧视频完整流程通过；正式构建现在强制执行冻结编辑器验收，最终交付证据见VALIDATION.md。
